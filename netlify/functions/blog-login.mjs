import{cookieName}from"./_blog-auth.mjs";import{text}from"./_blog-data.mjs";
export async function handler(e){if(e.httpMethod!=="POST")return text(405,"Method not allowed");const f=new URLSearchParams(e.body||""),u=process.env.URL||`https://${e.headers.host}`,r=await fetch(`${u}/.netlify/identity/token`,{method:"POST",headers:{"content-type":"application/x-www-form-urlencoded"},body:new URLSearchParams({grant_type:"password",username:f.get("email")||"",password:f.get("password")||""})});if(!r.ok)return text(401,"The email or password was not accepted.");const s=await r.json();return{statusCode:303,headers:{location:"/admin/","set-cookie":`${cookieName}=${encodeURIComponent(s.access_token)}; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=${s.expires_in||3600}`},body:""}}
